<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.winter.jisu.member.MemberMapper">
  
  
    <insert id="join" parameterType="com.winter.jisu.member.MemberDTO">
        INSERT INTO "MEMBER" (ID, PW, NAME, EMAIL, REG_DATE)
        VALUES (#{id}, #{pw}, #{name}, #{email}, SYSDATE)
    </insert>
  
    <select id="login" parameterType="String" resultType="com.winter.jisu.member.MemberDTO">
        SELECT ID, PW, NAME, EMAIL, REG_DATE 
        FROM "MEMBER" 
        WHERE ID = #{id}
    </select>

</mapper>